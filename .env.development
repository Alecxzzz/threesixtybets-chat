# En desarrollo usamos el backend local (uvicorn en localhost:8000)
VITE_API_URL=http://localhost:8000
# En desarrollo el proxy HLS lo provee el plugin de Vite (vite-plugins/hls-proxy.js)
# Al dejar VITE_HLS_PROXY vacío, stream.js usa "/hls-proxy" (local).
VITE_HLS_PROXY=
